/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.imgur.com', 'placehold.co', 'www.image2url.com'],
  },
}

module.exports = nextConfig